version: '3.8'

services:
  scraper-api:
    build: .
    container_name: markdown-scraper-api
    restart: always
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
